import com.hand.guvnor.Message;

rule "Hello World"
    when
     m : Message( status == 0 )
    then

     m.setMessage( "hello" );

     update( m );
     end


rule "GoodBye"
    when
     Message( status == 1 )
    then
    m.setMessage( "goodbye" );

    end